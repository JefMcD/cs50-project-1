#header1
a line containing a #header tag
#Header tag at beginning and #inline tags# later
this line has #one# then #another# headers
#a header# at the start #then another# later on
#line 2# has more #spanning words# with one at the start
